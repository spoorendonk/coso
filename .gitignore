# Build
build/
build-*/
compile_commands.json

# C++
*.o
*.a

# Python
__pycache__/
*.pyc
*.so
*.pyd
*.egg-info/
.pytest_cache/
dist/
.venv/

# Tools
.cache/
.vscode/

# Agent tooling — local to each checkout, not part of the published project.
# A fresh clone or worktree therefore has no /review or /start command and no
# statusline; the git hooks in .githooks/ are the gates that always run.
.claude/

# Benchmark instances (downloaded, not in git)
tests/data/*.vrp
tests/data/*.txt
tests/data/taillard/
tests/data/psplib/
tests/data/nrp/
